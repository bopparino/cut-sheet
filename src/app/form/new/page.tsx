export default function NewCutsheetPage() {
  return (
    <div className="space-y-2">
      <h1 className="text-2xl font-semibold tracking-tight">New Cutsheet</h1>
      <p className="text-muted-foreground">
        Form UI lands in the next iteration. Schema and persistence are wired; this stub keeps
        the route resolvable so we can deploy the scaffold.
      </p>
    </div>
  );
}
